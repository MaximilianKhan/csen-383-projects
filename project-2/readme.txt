## To run the code:
1. Go to the directory through the terminal (use `cd directory_path` command) and enter following commands:
	```
	$ gcc *.c -o program_output
	$ ./program_output
	```
2. To write the output to a file, execute as:
	```
	$ ./program_output.out > output1.txt
	```
